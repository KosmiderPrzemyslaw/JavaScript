const printTable = function (array) {
    for(let i = 0; i < array.length; i ++) {
        console.log(array[i])
    }
};

const fruits = ["jabłko", "banan", "pomarańcz", "brzoskwinia"];
printTable(fruits);
